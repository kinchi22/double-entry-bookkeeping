// VIOLATION: page metadata is copy too -- the browser shows the title -- and it
// is not JSX, so it needs its own clause. Expected gate: eslint, rule
// repo/no-inline-copy.
export const metadata = {
  title: 'Double Entry Bookkeeping',
};
