// Support file, not a violation. Gives the fixtures in this folder something
// local to import so they do not have to reach across a boundary to break an
// unrelated rule.
export type LedgerAmount = number;

export const LEDGER_ZERO: LedgerAmount = 0;
