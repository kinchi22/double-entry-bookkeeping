# Refuse to silently run under a different package manager.
engine-strict=true
# Supply-chain settings (minimumReleaseAge et al.) live in pnpm-workspace.yaml,
# which is where pnpm 11 reads configuration from.
